[package]
name = "epoch-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
epoch-common = { path = "../common" }
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
chrono = "0.4"
