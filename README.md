# Epoch

A personal time tracker built in Rust. CLI client talks to a REST API server (axum) backed by Postgres, all running on a self-managed Kubernetes cluster.

Also a vehicle for learning production infrastructure from scratch — auth, CI/CD, monitoring, backups, and more. See [project.md](project.md) for the full roadmap.
